import { gradientColors } from '~theme/color';
import { opacities } from '~theme/layout';

export const LINE_HEIGHT = 1.6;
export const FONT_SIZE_UNIT = 1.6;

export const SIZE_UNIT = 4;
export const OPACITY_UNIT = 0.16;

export const TRANSPARENT = 'transparent';
export const DEFAULT_GRADIENT_COLOR = gradientColors[12];

export const GRADIENT_START = { x: 0, y: 0 };
export const GRADIENT_END = { x: 1, y: 1 };

export const DEFAULT_OPACITY = opacities(5.5);
