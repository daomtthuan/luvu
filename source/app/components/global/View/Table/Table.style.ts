import { StyleSheet } from 'react-native';

export const TableViewStyles = StyleSheet.create({
  contentColumnContainer: {},
  contentRowContainer: {},
  fieldContainer: {
    backgroundColor: 'red',
  },
});
