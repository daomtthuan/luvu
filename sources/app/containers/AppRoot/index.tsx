import { memo } from 'react';

import { AppRootStyles } from './.style';
import { AppRootComponent } from './.type';

const styles = AppRootStyles;

const AppRoot: AppRootComponent = memo(function () {
  return <></>;
});

export { AppRoot };
